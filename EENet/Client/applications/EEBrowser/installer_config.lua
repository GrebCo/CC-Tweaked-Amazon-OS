-- Configuration for Installer and Updater
-- Easily change which branch and repository to pull from

config = {
    -- GitHub repository information
    owner = "GrebCo",
    repo = "CC-Tweaked-Amazon-OS",
    branch = "EEnetv2",  -- Change this to switch branches

    -- The base path in the repository (where EENet/Client is located)
    basePath = "EENet/Client",
}
